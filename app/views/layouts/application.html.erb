<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "G Books" %></title>
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "G Books" %>">
  <%= stylesheet_link_tag "application", media: "all" %>
  <%= javascript_include_tag "application" %>
  <script type="text/javascript" src="https://chalkable.com/Scripts/api/chlk-post-message-api.js"></script>
  <%if @show_plus %>
      <script type="text/javascript">
          CHLK_MESSENGER.addYourself(function saveAndClose(){
              return true;
          });

      </script>
  <% end %>

  <%= csrf_meta_tags %>
</head>
<body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>

     <!--<main role="main">-->


      <div id="app">
        <div id="container">
          <%= render 'layouts/messages' %>
        <%= yield %>
        </div>
      </div>
    <!--</main>-->
</body>
</html>
